-
  name: Automate application deployment
  hosts: centos
  vars:
    packages:
      - firewalld
      - httpd
      - mariadb
      - mysql-server

  tasks:
      -
        package:
          name: "{{ item }}"
          state: present
        with_items: "{{ packages }}"



